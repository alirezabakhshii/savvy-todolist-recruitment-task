import React, { Fragment } from "react";
import { TodoDialogModal } from "../TodoList/TodoDialogModal/TodoDialogModal";

export const Modals: React.FC = () => {
  return (
    <Fragment>
      <TodoDialogModal />
    </Fragment>
  );
};
